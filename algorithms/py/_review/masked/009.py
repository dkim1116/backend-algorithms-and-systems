# Description
# Return True if the binary tree satisfies strict binary search tree ordering rules at every node.
# Otherwise return False.
#
# Examples
# Input: root = [2, 1, 3]
# Output: True
# Input: root = [5, 1, 4, null, null, 3, 6]
# Output: False
#
# Constraints
# - Inputs follow the shapes shown in the examples.
# - Use the standard node-based structure implied by the prompt when one is needed.
# - Return exactly the value, collection, or boolean requested.
# - Assume the input size can be large enough that inefficient brute-force approaches may time out.
